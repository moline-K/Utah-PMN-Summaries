name: agenda_downloader
services:
  agenda_downloader:
    build: .
    command: python /app/agenda_downloader.py
    env_file:
      - .env
    environment:
      - PMN_CONFIG_PATH=/app/pmn_sources.example.yaml
      - DB_PATH=${DB_PATH:-/data/utah_pmn.db}
    volumes:
      - ./data:/data
      - ./pmn_sources.example.yaml:/app/pmn_sources.example.yaml:ro
    restart: unless-stopped

  agenda_summarizer:
    build: .
    command: python /app/run_summarizer.py
    env_file:
      - .env
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - PMN_CONFIG_PATH=/app/pmn_sources.example.yaml
      - DB_PATH=${DB_PATH:-/data/utah_pmn.db}
      - PROMPT_TEMPLATE_PATH=${PROMPT_TEMPLATE_PATH:-/app/prompt_template.default.txt}
    volumes:
      - ./data:/data
      - ./pmn_sources.example.yaml:/app/pmn_sources.example.yaml:ro
    restart: unless-stopped
